name: Z3 Coverage Daily Check

on:
  workflow_dispatch:
  schedule:
    - cron: "20 1 * * *"

jobs:
  check:
    uses: ./.github/workflows/solver-coverage-daily-check.yml
    with:
      solver_name: z3
      contract_path: contracts/solvers/z3.yml
    secrets: inherit
