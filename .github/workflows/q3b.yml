name: Build Q3B

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '0 * * * *'  # Run every hour

jobs:
  check-and-build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      
      - name: Restore solver state cache
        uses: actions/cache@v4
        id: cache-restore
        with:
          path: .cache
          key: solver-q3b-latest
          restore-keys: |
            solver-q3b-
      
      - name: Check for upstream changes
        id: check-upstream
        run: |
          ./scripts/check-upstream.sh q3b https://github.com/usi-verification-and-security/Q3B.git
          echo "build_needed=$(cat .build_status | grep build_needed | cut -d'=' -f2)" >> $GITHUB_OUTPUT
          echo "sha=$(cat .build_status | grep sha | cut -d'=' -f2)" >> $GITHUB_OUTPUT
      
      - name: Build Q3B
        if: steps.check-upstream.outputs.build_needed == 'true'
        run: ./scripts/q3b/build.sh

      - name: Persist built SHA
        if: steps.check-upstream.outputs.build_needed == 'true'
        run: |
          mkdir -p .cache
          printf '%s\n' "${{ steps.check-upstream.outputs.sha }}" > .cache/q3b_last_sha

      - name: Save solver state cache
        if: steps.check-upstream.outputs.build_needed == 'true'
        uses: actions/cache@v4
        with:
          path: .cache
          key: solver-q3b-${{ steps.check-upstream.outputs.sha }}
      
      - name: Skip build (up to date)
        if: steps.check-upstream.outputs.build_needed == 'false'
        run: echo "✅ Q3B is up to date - skipping build"
