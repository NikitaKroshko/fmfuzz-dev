name: CVC5 Coverage Mapper

on:
  workflow_call:
    inputs:
      cvc5_commit_hash:
        description: "CVC5 commit hash to build coverage for"
        required: false
        type: string
      test_count:
        description: "Pre-counted test count"
        required: false
        type: number
    secrets:
      AWS_ACCESS_KEY_ID:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true
      AWS_REGION:
        required: true
      AWS_S3_BUCKET:
        required: true
  workflow_dispatch:
    inputs:
      cvc5_commit_hash:
        description: "Optional: CVC5 commit hash to build coverage mapping for"
        required: false
        type: string

jobs:
  map:
    uses: ./.github/workflows/solver-coverage-mapper.yml
    with:
      solver_name: cvc5
      contract_path: contracts/solvers/cvc5.yml
      commit_hash: ${{ inputs.cvc5_commit_hash }}
      test_count: ${{ inputs.test_count || 0 }}
    secrets: inherit
